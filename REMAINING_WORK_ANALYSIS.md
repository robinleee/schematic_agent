# schematic_agent 项目待完成工作分析报告

> 生成日期: 2026-05-17  
> 分析人: Jarvis  
> 项目路径: `/data/schematic_agent`  
> 最新 Commit: `8d70e2d`  
> 代码规模: ~15,000 行 Python

---

## 一、项目现状速览

### 1.1 服务状态（2026-05-17 检查）

| 服务 | 状态 | 说明 |
|------|------|------|
| Neo4j | ❌ 未运行 | bolt://localhost:7687 连接被拒，需手动启动 |
| Ollama (gemma4:26b) | ✅ 运行中 | http://localhost:11434 |
| ChromaDB | ❌ 未运行 | http://localhost:8000 无响应 |
| Streamlit | ❌ 未运行 | http://localhost:8501 无响应 |

### 1.2 代码状态

| 指标 | 数值 |
|------|------|
| 总代码行数 | ~15,000 行 |
| Git 未提交修改 | 8 个文件 |
| 未跟踪文件 | 6 个（含 chroma_data/、新模块） |
| 模块数 | ETL(11) + Agent(17) + Web UI(3) |

### 1.3 已完成里程碑

| 阶段 | 内容 | 状态 |
|------|------|------|
| A 阶段 | 数据底座修复（Pin.Type 100%、POWERED_BY 955 条、PartType 99.1%） | ✅ |
| B 阶段 | LLM 客户端统一封装 | ✅ |
| C1 阶段 | Web UI 启动验证 | ✅ |
| 知识库管理 | Phase 1-6（DesignGuide/Checklist 解析→存储→UI→ETL 导入） | ✅ |

---

## 二、待完成工作全景

按优先级 P0 > P1 > P2 > P3 排列，共 **22 项**待完成。

---

### P0 — 阻塞性缺陷（3 项，预估 7-10 天）

> 不解决这些，系统核心能力受限。

#### 1. 知识库为空 — ChromaDB 仅 3 条记录

- **现状**: 知识库管理 UI 和解析管道已完成，但从未批量导入真实 Datasheet
- **影响**: Tier 1 检索始终返回空，Agent 的 spec_query 和诊断能力退化为纯图谱查询
- **根因**: 无批量导入脚本；解析管道已就绪但未执行
- **预估**: 2-3 天
- **方案**: 
  1. 收集 20-50 份 Datasheet PDF
  2. 编写批量导入脚本调用 `datasheet_parser.py` + `GraphRAGBridge`
  3. 验证检索质量（召回率、相关度）

#### 2. 嵌入模型不正规 — 4 种不兼容的向量空间

- **现状**: 代码中存在 4 种嵌入方式：
  - `knowledge_router.py` — 字符频率哈希 (512-dim)
  - `graph_rag_bridge.py` — 加权哈希 (768-dim)
  - `graph_rag_bridge.py` — Ollama gemma4:26b 嵌入
  - `graph_rag_bridge.py` — sklearn TF-IDF (768-dim)
- **影响**: 不同方法生成的向量互不兼容，查询和索引必须同源；哈希向量无语义意义
- **预估**: 3-5 天
- **方案**:
  1. 安装 `sentence-transformers` + `bge-large-zh-v1.5`
  2. 统一嵌入接口，废弃哈希/TF-IDF
  3. 重新索引所有知识库内容
  4. 远期迁移到 LlamaIndex + Neo4j Vector Index

#### 3. AMR 电容降额检查不可用

- **现状**: `CapacitorVoltageChecker` 存在但 `AMRDataSource.get_capacitor_voltage_rating()` 始终返回 None
- **影响**: 5 大审查模板之一（AMR）功能缺失，电容耐压降额是最常见的可靠性检查
- **根因**: 
  - 无真实 Datasheet 经过 HITL 审批流程
  - `amr_data.yaml` 为空或不存在
  - 端到端循环（PDF→提取→审批→写 YAML→AMR 读取）未验证
- **预估**: 2-3 天
- **方案**:
  1. 导入 10-20 份电容 Datasheet
  2. 走通完整 HITL 审批流
  3. 验证 `amr_data.yaml` 读取逻辑
  4. 修复发现的断裂点

---

### P1 — 核心功能完善（8 项，预估 18-25 天）

#### 4. 复合意图未执行为任务队列

- **现状**: `LLMIntentRouter` 能识别 `COMPOSITE` 意图并提取 `sub_intents`，但 `agent_core.py` 将其映射为 `SPEC_QUERY`，丢弃了分解结果
- **影响**: 用户问多部分问题只得到不完整答案
- **预估**: 3-4 天

#### 5. Agent ReAct 循环仅诊断可用

- **现状**: `ReActDiagnosisEngine` 实现了完整 ReAct，但仅用于诊断任务；审查和查询仍用硬编码状态机
- **影响**: 维护两套执行路径；审查任务中的硬编码检查（如 `if "R30002" in i2c_info`）是 Beet7 特有的
- **预估**: 2-3 天
- **方案**: 将 ReAct 扩展到所有任务类型，移除硬编码检查

#### 6. 网络电压标注率仅 17.3%

- **现状**: `VoltageLevelExtractor` 支持约 10 种模式，仅 1,416/8,159 网络有电压标注
- **影响**: 电源域分析和 AMR 检查对 82.7% 的网络不生效
- **预估**: 1-2 天
- **方案**: 扩展模式列表（VDDQ, VPP, VTT, 1V0_DDR, VCC_WL_1V8 等 20+ 模式）

#### 7. 推理链路可视化缺失

- **现状**: 后端 `AgentState.execution_trace` 数据完整，但 Web UI 仅显示最终报告文本
- **影响**: 工程师无法验证 Agent 的推理过程，影响信任度和可调试性
- **预估**: 1-2 天

#### 8. Neo4j 图谱可视化缺失

- **现状**: 无 PyVis/ECharts 集成，图谱查询仅返回文本
- **影响**: 图谱拓扑是核心价值，纯文本输出限制了工程师使用体验
- **预估**: 2-3 天

#### 9. BOM CSV 导入未实现

- **现状**: ETL 导入仅支持 Cadence 网表，无 CSV BOM 导入
- **影响**: 无法导入 MPN、Description、Manufacturer 信息，限制了 PartType 分类和 AMR 参数查找
- **预估**: 3-4 天

#### 10. Tier 2/3 知识检索未连接

- **现状**: `KnowledgeRouter.tier2 = None`，`PublicMPNRetriever.enabled = False`
- **影响**: Tier 1 为空时无后备，冷启动问题
- **预估**: 2-3 天/层

#### 11. 规则误报调优

- **现状**: EXTERNAL_IO_ESD 438 误报、NC_FLOATING_CHECK 500 误报
- **影响**: 审查结果可信度下降
- **预估**: 1-2 天

---

### P2 — 生产化部署（7 项，预估 10-14 天）

#### 12. vLLM 部署替代 Ollama

- **现状**: Ollama 响应 ~10s/次，不支持并发
- **预估**: 1 天

#### 13. 单元测试覆盖率 < 40%

- **现状**: 约 1,124 行测试代码，5 个文件；PRD 要求 > 60%
- **预估**: 3-5 天

#### 14. 端到端集成测试缺失

- **现状**: 无自动化 E2E 测试；`run_etl_validation.py` 仅验证 ETL
- **预估**: 2-3 天

#### 15. Cypher 性能优化

- **现状**: GND 等大网络查询可能超时；PRD 要求 < 500ms
- **预估**: 0.5 天

#### 16. LLM 响应缓存

- **预估**: 0.5 天

#### 17. 规则热更新

- **现状**: `ReviewRuleEngine` 在 `__init__()` 加载规则，审批后需重启
- **预估**: 1 天

#### 18. Neo4j 只读账号

- **现状**: 使用 neo4j 管理员账号全权访问
- **预估**: 0.5 天

---

### P3 — 高级功能（4 项）

#### 19. True GraphRAG（LlamaIndex 完整集成）

- **现状**: 自定义 `GraphRAGBridge` 替代，功能有限
- **预估**: 5 天

#### 20. 差分对追踪（PCIe/MIPI）

- **现状**: `trace_differential_pair()` 是占位符
- **预估**: 3-5 天

#### 21. 共因失效定位

- **预估**: 3 天

#### 22. 多网表/多项目支持

- **预估**: 5 天

---

## 三、架构技术债

### 3.1 双向量存储未同步

ChromaDB（`knowledge_router`）和 Neo4j VectorChunk（`graph_rag_bridge`）独立存储嵌入向量，不同步。查询路径取决于调用哪个模块。

**建议**: 统一为单一向量存储，优先 Neo4j VectorChunk（单库简洁），或远期迁移到 LlamaIndex。

### 3.2 嵌入策略碎片化

4 种不兼容的向量空间（字符哈希、加权哈希、Ollama、TF-IDF），不同方法索引的内容无法交叉查询。

**建议**: 统一为 `bge-large-zh-v1.5`，废弃所有哈希/TF-IDF 方法。

### 3.3 硬编码状态机 vs ReAct 双路径

诊断用 ReAct，审查/查询用硬编码。审查 `tool_executor_node` 中有 Beet7 特定硬编码检查。

**建议**: ReAct 扩展到所有任务，移除硬编码。

### 3.4 LLMClient 双导入

`OllamaClient` 兼容包装器 + `LLMClient` 并存，造成混淆。

**建议**: 移除 `OllamaClient`，全局使用 `LLMClient`。

---

## 四、环境待修复项

| 问题 | 说明 | 修复方式 |
|------|------|---------|
| Neo4j 未运行 | 机器重启后未自启 | `$NEO4J_HOME/bin/neo4j start` |
| ChromaDB 未运行 | 同上 | `chroma run --host 0.0.0.0 --port 8000` |
| Streamlit 未运行 | 同上 | `streamlit run web_ui/app.py` |
| 8 个文件未提交 | 含修改和新模块 | 评估后 commit 或清理 |

---

## 五、建议执行顺序

### 第 1 周：环境恢复 + P0 启动

| 天 | 任务 | 产出 |
|----|------|------|
| D1 | 启动所有服务 + 验证数据完整性 | 服务就绪 |
| D2-3 | 安装 `bge-large-zh-v1.5` 嵌入模型，统一嵌入接口 | 统一向量空间 |
| D4-5 | 批量导入 Datasheet + 验证检索质量 | 知识库可用 |

### 第 2 周：P0 收尾 + P1 快赢

| 天 | 任务 | 产出 |
|----|------|------|
| D1-2 | AMR 电容降额端到端验证 | AMR 检查可用 |
| D3 | 网络电压标注扩展（17.3% → 80%+） | 电压覆盖提升 |
| D4 | 推理链路可视化 | Web UI 推理展示 |
| D5 | 规则误报调优 | 审查可信度提升 |

### 第 3-4 周：P1 核心功能

| 天 | 任务 |
|----|------|
| D1-3 | 复合意图任务队列 |
| D4-5 | ReAct 扩展到审查/查询 |
| D6-7 | Neo4j 图谱可视化 |
| D8-10 | BOM CSV 导入 |

### 第 5-6 周：P2 生产化

- vLLM 部署
- 单元测试 + E2E 测试
- 规则热更新
- Neo4j 只读账号

---

## 六、PRD 完成度总结

| PRD 模块 | 完成度 | 关键差距 |
|----------|--------|---------|
| §4 数据底座 (ETL) | 85% | BOM 导入缺失，电压标注低 |
| §5 Agent 核心 | 75% | 复合意图未执行，ReAct 仅诊断 |
| §6 图谱工具 | 90% | 差分对占位符 |
| §7 知识系统 | 40% | 知识库为空，嵌入碎片化，无 LlamaIndex |
| §7 HITL | 70% | 管道存在但未用真实数据验证 |
| §8/10 Web UI | 65% | 无图谱可视化、无推理可视化 |
| §12 质量保障 | 30% | 无只读账号，测试覆盖率低 |

**总体完成度: ~65%**

核心框架和管道已建立，但 **知识库为空** 和 **嵌入碎片化** 是当前最大的系统性阻塞。解决这两个问题后，系统将具备实际可用的智能审查能力。
